'use client';


export function PreloadResources() {
    // ReactDOM.preload('sprite.svg', { as: 'image' });
    // ReactDOM.preload("https://example.com/font.woff2", { as: "font" });
    return null;
}
